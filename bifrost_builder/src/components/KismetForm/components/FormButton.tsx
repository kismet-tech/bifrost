import styled from "styled-components";

export const FormButton = styled.button`
  cursor: pointer;
  flex: 1;
  padding: 16px;
  border: none;
  // This should be configurable by the client
  background-color: rgb(156, 154, 129);
  color: white;
  border-radius: 6px;
`;
